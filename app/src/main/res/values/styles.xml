<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/lux_rose</item>
        <item name="colorPrimaryDark">@color/lux_rose</item>
        <item name="colorAccent">@color/lux_purple</item>

        <item name="android:statusBarColor">@color/lux_rose</item>
        <item name="android:navigationBarColor">@color/lux_dark</item>
        <item name="android:windowLightStatusBar">false</item>

        <item name="android:fontFamily">sans</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>